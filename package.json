{
  "name": "quizzazzle",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "esbuild": "^0.19.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-elastic-carousel": "^0.11.5",
    "styled-components": "^6.0.7"
  },
  "author": "",
  "license": "COPYRIGHT",
  "scripts": {
    "help": "npm run",
    "build": "esbuild app.jsx --bundle --outfile=build/out.js",
    "prod": "esbuild app.jsx --minify --sourcemap --bundle --outfile=build/out.js",
    "watch": "esbuild app.jsx --bundle --watch --outfile=build/out.js",
    "serve": "npx http-server -c-1 -p 3000"
  }
}
