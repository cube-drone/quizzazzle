{
  "name": "quizzazzle",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "@preact/compat": "^17.1.2",
    "animejs": "^3.2.1",
    "esbuild": "^0.19.2",
    "htm": "^3.1.1",
    "insane": "^2.6.2",
    "marked": "^9.1.2",
    "preact": "^10.18.1",
    "zingtouch": "^1.0.6"
  },
  "author": "",
  "license": "COPYRIGHT",
  "scripts": {
    "help": "npm run",
    "build": "esbuild app.js --bundle --outfile=build/out.js",
    "prod": "esbuild app.js --minify --sourcemap --bundle --outfile=build/out.js",
    "watch": "esbuild app.js --bundle --watch --outfile=build/out.js",
    "serve": "npx http-server -c-1 -p 3000",
	"test": "node --test"
  }
}
